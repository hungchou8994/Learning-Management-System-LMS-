import React from "react";

export const metadata = {
  title: "User Settings - SkillGro",
};

const SettingsLayout = ({ children }: { children: React.ReactNode }) => {
  return <div>{children}</div>;
};

export default SettingsLayout;
