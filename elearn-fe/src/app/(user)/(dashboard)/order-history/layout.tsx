import React from "react";

export const metadata = {
  title: "Order History | SkillGro",
};

const ProfileLayout = ({ children }: { children: React.ReactNode }) => {
  return <div>{children}</div>;
};

export default ProfileLayout;
